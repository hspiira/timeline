# Python-generated files
# (/docs/ was ignored here, which quietly broke every docs/ link in the README.)
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Environment files (never commit). Only .env.example is safe to track.
.env
.env.*
!.env.example
# Virtual environments
.venv

# Vercel
.vercel/

# Secrets directory and key files (never commit)
secrets/
serviceAccountKey*.json
*-firebase-adminsdk-*.json
# Common GCP/Google key file names
**/credentials.json
**/gcp-key*.json
**/google-credentials*.json
**/*-service-account-*.json
.env*.local

# Local development files
.vscode/
.idea/
.pytest_cache/
.ruff_cache/
.mypy_cache/
.coverage
.coverage.*
.coverage.xml
.coverage.json
.coverage.json.gz
.coverage.json.bz2

# Storage
storage/

# graft's local graph cache — regenerable, not committed (run `graft build`).
/graft/
